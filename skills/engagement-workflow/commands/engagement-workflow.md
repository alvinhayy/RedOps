---
description: Build a phase-gated RedOps engagement plan for an authorized task.
---

Use the `engagement-workflow` skill. Call `plan_engagement` first, then `route_task`
for niche agents. Keep exploitation, post-exploitation, and lateral movement blocked
until written scope and exact approval are confirmed. Preserve source citations and
handoff artifacts; never claim execution without command output.

Task: $ARGUMENTS
