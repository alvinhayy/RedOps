#!/bin/sh
set -eu

SCRIPT_PATH=$0
while [ -L "$SCRIPT_PATH" ]; do
    LINK=$(readlink "$SCRIPT_PATH")
    case "$LINK" in
        /*) SCRIPT_PATH=$LINK ;;
        *) SCRIPT_PATH=$(dirname "$SCRIPT_PATH")/$LINK ;;
    esac
done
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$SCRIPT_PATH")" && pwd)
exec python3 "$SCRIPT_DIR/claude-zai.py" "$@"
